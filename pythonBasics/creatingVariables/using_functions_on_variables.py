string_variable = "This is my String variable"
int_variable = 238
float_variable = 15.5
boolean_variable = True

# Using functions

print(len(string_variable)) # Printing the lenght of string_variable

print(type(str(int_variable))) # str() function converts a type into its string representation

# We can ask the user to input a variable

first_name = input('My name is: ')

# And we can convert that type if we need a specific one

surname = str(input('My surname is: '))

# And we can stablish a initial type

address: str = "Apple avenue"